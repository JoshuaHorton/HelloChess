# Major Refactoring Tasks

## Phase 1: Fast Core Backend
- [ ] Create `FastBoard` class (`int[64]` representation)
- [ ] Create `MoveGenerator` (standard rules, en passant, castling)
- [ ] Update `Game.java` to route `processMove` to `FastBoard`
- [ ] Delete old `HashMap` and `Position` classes

## Phase 2: Premium Swing UI
- [x] Generate minimal 2D chess piece assets
- [ ] Slice the generated sprite sheet in Java
- [ ] Create `BoardPanel` with `Graphics2D` custom rendering (gradients, shadows)
- [ ] Implement smooth piece sliding animations (`Timer`)
- [ ] Implement drag-and-drop ghosting and legal move highlights
- [ ] Replace `ChessGUI` board grid with `BoardPanel`

## Phase 3: Puzzle Generator
- [ ] Build `AlphaBetaSearch` for move evaluation
- [ ] Implement FEN string parser
- [ ] Create internal random-play loop to mine for "Mate in 3" puzzles
- [ ] Add "Puzzle Mode" UI to Swing app
